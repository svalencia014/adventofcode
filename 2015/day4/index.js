const fs = require('fs');
const input = fs.readFile('./input.txt', (error, data) => {
  if (error) { console.error(error); return; }
  let correctHash = false;
  for (let i = 0; !correctHash; i++) {
    
  }
})